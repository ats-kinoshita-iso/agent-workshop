{
  "hooks": [
    {
      "event": "PreToolUse",
      "matcher": "Bash(git commit*)",
      "type": "command",
      "command": "uv run ruff format --check . && uv run ruff check . && uv run mypy . && uv run pytest -x -q"
    },
    {
      "event": "Stop",
      "type": "command",
      "command": "echo '--- Quality Summary ---' && uv run ruff format --check . 2>&1 | tail -1 && uv run ruff check . 2>&1 | tail -1"
    }
  ]
}
